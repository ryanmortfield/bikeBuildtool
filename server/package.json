{
  "name": "bike-build-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create bike-build-db",
    "db:migrate": "wrangler d1 migrations apply bike-build-db"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "elysia": "^1.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.94.0"
  }
}
